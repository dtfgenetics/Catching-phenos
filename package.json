{
  "name": "phenoquest-living-seed-vault",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "smoke": "node scripts/smoke-check.mjs",
    "compile": "node scripts/compile-check.mjs",
    "contracts": "node scripts/data-contract-check.mjs",
    "rules": "node scripts/core-rules-check.mjs",
    "audit": "node scripts/repo-audit.mjs",
    "first-session": "node scripts/first-session-check.mjs",
    "build": "node scripts/build-public.mjs && node scripts/verify-public-build.mjs",
    "verify:build": "node scripts/verify-public-build.mjs",
    "check": "npm run audit && npm run contracts && npm run rules && npm run smoke && npm run compile && npm run first-session && npm run build",
    "dev": "node scripts/dev-server.mjs"
  }
}
